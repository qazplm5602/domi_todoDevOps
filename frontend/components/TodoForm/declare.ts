export interface TodoData {
    id: number,
    title: string,
    description: string,
    startDate: string
}

export interface TodoPreview {
    id: number,
    title: string,
    startDate: string
}